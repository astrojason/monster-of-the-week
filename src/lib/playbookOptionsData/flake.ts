import type { PlaybookOptionSection } from "../playbookOptions";

// The Flake has no character-creation sections beyond the standard
// Look/Stats/Moves/Gear pattern (verified against the PDF). "Get a haven
// like the Expert's" is a level-up improvement, not a creation-time pick.
export const flake: PlaybookOptionSection[] = [];
